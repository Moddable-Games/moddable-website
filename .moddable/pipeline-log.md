# Moddable Games — Pipeline Log

This file records quality assessments of routine runs. Written by Triage after reviewing Research and Implementation output. It is the feedback loop that allows the pipeline to improve over time.

Read this at the start of every Desktop session and every Triage run.

---

## Format

Each entry covers one routine run. Triage writes entries for the Research and Implementation runs it is reviewing. Desktop sessions may add entries when reviewing merged PRs.

```
### {date} — {Routine name} — {Issue worked}
**Output:** [what was produced]
**Quality:** Pass / Fail / Partial
**Notes:** [what was good, what was thin, what was missing, scope creep, pattern violations]
**Action taken:** [sent back to research / merged / flagged needs-decision / etc]
```

---

## 2026-06-16 — Implementation Routine — chess#101 (SVG board renderer)

**Output:** `js/svg-renderer.js` — DOM-free SVG string builder for chess and draughts boards. Three board styles: checkered, mono-grid (Turkish Draughts), alquerque. Piece rendering for chess (unicode) and draughts (circles). PR#102 merged to dev.
**Quality:** Pass
**Notes:** Solid output. Matched the spec. No scope creep. One issue: PR description included a `_Generated by Claude Code_` footer — this predates the explicit footer ban added to conventions.md at 09:52 on this date. Not a current bug.
**Action taken:** Merged to dev. Closes chess#101, unblocks rules#57.

---

## 2026-06-16 — Implementation Routine (webhook) + Implementation B (webhook) — website#86 (remove placeholder Nukes mods)

**Output:** Two identical PRs (#110, #111) — both removed placeholder community mods from `games-content.json` and `games/nukes/index.html`. Both merged to dev within seconds of each other.
**Quality:** Partial — correct output, wasted run
**Notes:** Double-fire caused by both routines having webhook triggers on moddable-website. Triage applied a `ready` label at 08:05, both fired simultaneously at 08:08. Net result on dev is correct. Side effect: `games-content.json` was reformatted wholesale (unicode escapes → literal UTF-8, multi-line arrays compressed) when only a targeted removal was needed — produced a 400-line diff. Character encoding and file editing discipline rules added to conventions.md as a result. PR footers present on both — pre-conventions-update artefact.
**Action taken:** Both merged. Webhooks removed from all 4 routines to prevent recurrence. conventions.md updated with encoding standards and file editing discipline rule.

---

## 2026-06-15 — Implementation (Desktop) — rules#62 (SVG diagram alignment)

**Output:** Fixed SVG board diagram layout across Mancala, Backgammon, Morris, and Royal Ur variant pages. Symmetric pit spacing, corrected viewBoxes, improved piece contrast. PR#62 merged.
**Quality:** Pass — correct output.
**Notes:** Convention violation: PR merged directly to `main` instead of `dev`. All work — automated or Desktop — must target `dev` in moddable-rules. Flagged for awareness; content is correct on production but the branch rule was not followed.
**Action taken:** Merged to main (incorrectly). Content correct. No remediation action; noting the pattern violation for future reference.

---

## 2026-06-16 — Implementation (Desktop) — chess#103 (chess_render_svg MCP tool)

**Output:** `js/chess-svg.js` — pure string-builder SVG renderer, no DOM dependencies. Wired into `mcp/tools.js` as `chess_render_svg`. Committed in 772fbb0. Supports all board sizes, 6 themes, optional highlight squares. Works in Cloudflare Workers.
**Quality:** N/A — committed directly without a PR; diff cannot be assessed from this run.
**Notes:** Implementation done outside the standard routine pattern (direct commit, no PR branch). Not a regression risk for a new file, but the PR workflow is the expected pattern for auditability. Issue closed without a PR reference.
**Action taken:** Issue closed as completed.

---

## 2026-06-17 — Research (Desktop) — rules#65 (The Landlord's Game)

**Output:** Full architecture spec + all source content embedded in issue comments. Six variants specified: 1904 Original, 1906 Commercial, Single Tax, Prosperity, Monarch of the World, Atlantic City. Implementation is self-contained — no external URL access required by the implementation routine.
**Quality:** Pass — all quality gate criteria met.
**Notes:** Two minor issues: (1) Comment 1 appends a generated-by footer (`_Generated by [Claude Code](https://claude.ai/code)_`) — violates current conventions. Pre-existing, cannot be edited. (2) Comment 5 claims "Full text embedded above" for the Monarch of the World variant but the specific 1906 rules section is not present in the source comments; the frontmatter special field is descriptive enough to implement the variant file. Neither blocks implementation.
**Action taken:** Already labelled `ready` + `next` by Mark. Implementation routine can proceed.

---

## 2026-06-17 — Research (Desktop) — rules#64 (Colony dead end)

**Output:** Colony / Catan determined to be a dead end. Catan is commercially locked (Kosmos/Asmodee trademark active); Colony is a Catan-style map generator with no associated open rulebook. Dead-ends.md updated with Catan entry.
**Quality:** N/A — dead end determination, not a spec.
**Action taken:** Issue closed as dead end. Inventory updated: Colony moved from queued to dead end.

---

## 2026-06-17 — Triage — All 6 repos

**Output:** Full triage pass across moddable-website, moddable-chess, moddable-hexmaps, moddable-rules, moddable-decks, dungeon-chess.
**Quality:** N/A
**Notes:** 1 `ready` issue passed the quality gate (rules#65). 6 `research` issues passed (chess#104, rules#66–70). No issues sent back. No stale issues found. No `discuss` issues flagged as resolved. Inventory updated. `next` applied to rules#66 (Pachisi). Pipeline log brought up to date with uncovered entries for rules#62, chess#103, rules#65, and rules#64.
**Action taken:** `next` added to rules#66. inventory.md committed to moddable-rules dev. pipeline-log.md committed to moddable-website dev. Triage summary posted on website#82.
